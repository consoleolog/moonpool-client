import {createSlice} from "@reduxjs/toolkit";
import {ProblemListType} from "../../types/ProblemTypes";

const problemListSlice = createSlice({
    name: "problemListSlice",
    initialState: [],
    reducers : {

    }
})

export default problemListSlice.reducer